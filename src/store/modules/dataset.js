const state = {
    visible : false
}

const mutations = {
    set_visible(state) {
        this.state.visible = true
    }
}

const actions = {
    
}

export default {
    state,
    mutations,
    actions
}